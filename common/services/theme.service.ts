// TODO: implement theme service (should move this into each repo scope bc of the different UI libs)
// import { ColorCode, ConfigTheme, Theme } from '@puravida/react-ui';

// const color: Partial<ColorCode> = {};

// const setColors = (colors: ColorCode) => Object.assign(color, colors);

// const theme: Partial<Theme> = {};

// const setTheme = (_theme: ColorCode) => Object.assign(theme, _theme);

// const config: Partial<ConfigTheme> = {};

// const setConfig = (_config: ConfigTheme) => Object.assign(config, _config);

// const setThemeService = (themeService: any) => {
//   setColors(themeService.color);
//   setTheme(themeService.theme);
//   setConfig(themeService.config);
// };

// export default {
//   color,
//   setColors,
//   theme,
//   setTheme,
//   config,
//   setConfig,
//   setThemeService,
// };
