// TODO: implement token service
// import { Auth } from 'aws-amplify';

// const getToken = () =>
//   new Promise((resolve, reject) => {
//     Auth.currentSession()
//       .then((session: any) => {
//         resolve(session.idToken.jwtToken);
//       })
//       .catch((err) => {
//         reject(err);
//       });
//   });

// const forceRefreshToken = async () => Auth.currentAuthenticatedUser({ bypassCache: true });

// export default {
//   getToken,
//   forceRefreshToken,
// };
